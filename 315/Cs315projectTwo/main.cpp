#include<iostream>
#include"reclists.hpp"
#include"solutions.hpp"
#include<cstring>

using namespace std;

int main()
{
/*
    std::cout << "Starting main..." << std::endl;
    list p;

    cout << "Enter a list to counts the number of atoms in it: ";
    p = read_list();
    cout << "The list has " << numAtoms(p) << " atoms at the top level.\n";

    // Write the code for each function, test it thoroughly, and then comment
    // it out and move to the next function.
*/

    cout << "enter a list" << endl;
    list p = read_list();
    cout << "Enter a second list" << endl;
    list q = read_list();

    cout << member((m) (a b c d e f g));
    //list r = shape(p);
    //write_list(r);

    //cout << equal(p,q) << endl;
    //write_list(repeatAtom(p,5));
    //cout << is_lat(p);
    return 0;
}
